const connection = require('../config/db-config');

const TABLE = 'saison_joueur';

const findSaisonJoueur = () => connection.promise().query(`SELECT * FROM ${TABLE}`);

module.exports = {
    findSaisonJoueur,
}